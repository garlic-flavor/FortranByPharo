"
SWFortranByPharo-Analyzer emulates execution stack of Pharo virtual machine.
And It detects exact types of all objects of Pharo.
The main usage is SWStackAnalyzer class>>#new:with: then SWStackAnalyzer>>#decode.
"
Class {
	#name : #ManifestSWFortranByPharoAnalyzer,
	#superclass : #PackageManifest,
	#category : #'SWFortranByPharo-Analyzer-Manifest'
}
